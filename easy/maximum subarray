class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        cur=nums[0]
        max_n=nums[0]
        for i in range(1,len(nums)):
            cur=max(nums[i],cur+nums[i])
            max_n=max(cur,max_n)
        return max_n