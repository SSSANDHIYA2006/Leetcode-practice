class Solution:
    def getRow(self, rowIndex: int) -> List[int]:
        l=[]
        for i in range(rowIndex+1):
            row=[1]*(i+1)
            for j in range(1,i):
                row[j]=l[i-1][j-1]+l[i-1][j]
            l.append(row)
        return l[rowIndex]