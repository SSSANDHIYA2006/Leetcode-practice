class Solution:
    def longestPalindrome(self, s: str) -> int:
        res = 0
        
        for i in set(s):
            c = s.count(i)
            res += (c // 2) * 2
        
        if res < len(s):
            res += 1
        
        return res